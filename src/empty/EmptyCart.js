import React from 'react';
import ReactDOM from 'react-dom';

const EmptyCart = (props) => {
	return(
		<div className="empty-cart">
			<p>Add some products in the cart <br/> 
				:)
			</p>
		</div>
	);
}

export default EmptyCart;